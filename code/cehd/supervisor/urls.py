from django.urls import path, include



urlpatterns = [
    # Student save time sheet URL
    # path('student/save/', CoopViewsSave.as_view(), name='coop_save_time_logs'),
    # student submit time sheet URL
]